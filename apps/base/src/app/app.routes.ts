import type { Routes } from '@angular/router';

/**
 * Base app routes — intentionally empty (the app shows only its header). Add
 * pages as lazy routes when scaffolding from this template, e.g.:
 *
 *   {
 *     path: '',
 *     loadComponent: () => import('./home/home.component').then((m) => m.HomeComponent),
 *   }
 */
export const appRoutes: Routes = [];
