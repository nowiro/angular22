/**
 * Public API for the wizard-form-fill library.
 *
 * Dev-only "fill the form" panel for Signal Forms wizards: each app provides
 * `WIZARD_FILL_PRESETS` (from `@angular22/wizard-core`) — a function that
 * swaps the form model for a preset — and the panel renders only on localhost.
 *
 * @packageDocumentation
 */
export { A22DevFillPanelComponent } from './lib/dev-fill-panel.component';
